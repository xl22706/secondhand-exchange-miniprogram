# Checklist

## 云开发环境配置
- [x] `miniprogram/app.js` 中 `env` 已配置为 `cloud1-d6g4fb9piee5ee36d`
- [ ] 数据库集合 `goods` 已创建，权限为"所有用户可读，仅创建者可写"
- [ ] 数据库集合 `exchanges` 已创建，权限为"自定义权限（仅云函数可读写）"
- [ ] 数据库集合 `users` 已创建，权限为"仅创建者可读写"
- [ ] `goods` 集合索引已按 spec 要求全部创建
- [ ] `exchanges` 集合索引已按 spec 要求全部创建
- [ ] `users` 集合索引已按 spec 要求全部创建

## 用户系统
- [x] `userFunctions` 云函数代码已实现，包含 `getUserInfo` 和 `updateUserInfo`
- [x] 小程序启动时自动调用 `getUserInfo` 获取并注册用户
- [x] 用户头像和昵称正确展示在"我的"页面

## 首页物品列表
- [x] 首页展示物品卡片列表，包含图片、标题、分类、成色
- [x] 分类筛选 Tab 可正常切换并刷新列表
- [x] 下拉刷新功能正常
- [x] 上拉加载更多功能正常，分页逻辑正确
- [x] 点击卡片正确跳转到物品详情页

## 物品详情页
- [x] 物品详情页正确展示图片轮播、标题、描述、成色、发布者信息
- [x] 本人发布的物品显示"编辑"和"删除"按钮
- [x] 他人发布的物品显示"申请交换"按钮
- [x] 点击"申请交换"弹出留言输入框，提交后调用云函数成功

## 发布物品页
- [x] 表单包含所有字段：标题、分类、成色、描述、图片上传、地点、期望交换
- [x] 图片上传至多 6 张，上传至云存储
- [x] 提交后调用 `createGoods` 成功，并跳转首页

## 个人中心
- [x] 展示用户头像和昵称
- [x] "我的物品"入口可正常跳转
- [x] "我的请求"入口可正常跳转

## 我的物品
- [x] 列表正确展示当前用户发布的所有物品
- [x] 编辑功能正常，可修改物品信息
- [x] 删除功能正常，删除后级联清理关联交换请求
- [x] 物品状态标签（可交换/已交换/已下架）显示正确

## 交换请求
- [x] `exchangeFunctions` 云函数代码已实现，所有接口可用
- [x] 不能对自己的物品发起交换请求
- [x] 不能重复申请同一物品
- [x] "我发出的请求"列表正确展示，状态显示正确
- [x] "我收到的请求"列表正确展示，可同意或拒绝
- [x] 同意请求后，对应物品状态自动变为 `exchanged`

## 全局检查
- [x] `app.json` 中页面路由和 TabBar 配置正确
- [ ] 所有云函数已通过"创建并部署：云端安装依赖"部署
- [x] 小程序编译无报错，各页面跳转正常
- [x] 数据换设备登录不丢失（验证云数据库持久化）
